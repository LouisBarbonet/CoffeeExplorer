# Environment variables
.env
.env.local

# IDE
.idea/

# OS
.DS_Store
Thumbs.db

# Local scratch space for scripts/backup.sh + scripts/restore.sh (dump
# files + the transient rclone config) -- never meant to be committed.
backups/
